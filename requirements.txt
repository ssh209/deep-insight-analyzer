# --- LLM 및 Agent 프레임워크 ---
google-genai==2.6.0          # Gemini API 호출 (engine.py 등)
google-cloud-storage>=2.14.0 # Vertex AI 연동용 Storage
google-cloud-aiplatform>=1.38.0 # Vertex AI Batch Prediction
langchain-core==1.4.0        # LangChain 핵심
langchain-community==0.4.2   # Chroma, HuggingFaceEmbeddings 등
langchain-huggingface>=0.1.0 # 최신 HuggingFace Embeddings 사용을 권장하는 신규 패키지
langgraph==1.2.1             # 8-agent 파이프라인 그래프 구성

# # --- 머신러닝 및 데이터 처리 ---
# lightgbm>=4.0.0,<4.6.0       # NVI 예측 모델
# scikit-learn>=1.3.0,<1.6.0   # ML 유틸리티
# pandas>=2.2.0,<2.3.0         # 데이터프레임 처리 (Python 3.13 호환)
# numpy>=1.26.0,<2.2.0         # 수치 연산
# joblib>=1.3.0                # pkl 모델 로드/저장
# statsmodels>=0.14.0,<0.15.0  # 통계 모델

# # --- TFT 예측 모델 (FORECASTER_MODEL=tft 시 필요) ---
# torch>=2.4.0                 # PyTorch (TFT 백엔드)
# pytorch-lightning>=2.4.0     # Trainer, callbacks
# pytorch-forecasting>=1.2.0   # TemporalFusionTransformer, TimeSeriesDataSet

# # --- MOIRAI 예측 모델 (FORECASTER_MODEL=moirai 시 필요) ---
# einops==0.7.*                # MOIRAI 내부 텐서 연산
# gluonts~=0.14.3              # 시계열 데이터 유틸리티 (PandasDataset)
# uni2ts                       # MOIRAI 2.0 모델 (Salesforce, pip install 후 별도 셋업 필요)

# # --- RAG (벡터 DB 및 임베딩) ---
chromadb==1.5.9              # 로컬 벡터 스토어
sentence-transformers==5.5.1 # HuggingFace 임베딩 모델 로드

# # --- UI 및 시각화 ---
streamlit==1.57.0            # 대시보드 UI
graphviz==0.21               # LangGraph 워크플로우 시각화

# --- 데이터베이스 및 유효성 검사 ---
asyncpg==0.30.0              # 비동기 PostgreSQL 드라이버
pydantic==2.13.4             # PipelineState 및 데이터 검증

# --- Testing ---
pytest>=8.0.0                # 유닛 테스트 프레임워크
pytest-asyncio>=0.23.0       # 비동기 테스트 지원
pytest-mock>=3.14.0          # Mocking 지원
